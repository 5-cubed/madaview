# Recognized markdown extensions

Madaview treats `.md`, `.markdown`, and `.mdx` as markdown files, matched case-insensitively (e.g. `README.MD` counts). This is the project's definition of "a markdown file" wherever filtering by file type is needed (e.g. the sidebar file tree, see [[20260711-191722-sidebar-markdown-only-filter]]).
