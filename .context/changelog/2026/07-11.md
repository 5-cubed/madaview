# 2026-07-11

- Initial project setup for madaview, a Go+React markdown viewer distributed as a single self-contained binary (`.context/adr/20260711-173722-madaview-initial-setup.md`). Implemented `internal/rootfs` (root resolution priority, symlink-aware path-traversal guard, atomic runtime root swap), `internal/config` (persisted root in the OS user-config dir), `internal/markdown` (goldmark + GFM + chroma syntax highlighting + custom Mermaid/KaTeX placeholder emission for client-side-only hydration + bluemonday sanitization), `internal/server` (`/api/tree`, `/api/file`, `/api/status`, `/api/root` JSON API + SPA serving + request/root-change logging), `internal/assets` (go:embed of the built frontend), and `cmd/madaview` (CLI entrypoint with `--root`/`--port`/`-v`).
- Built the React + Vite + Tailwind frontend: sidebar file tree (lazy per-folder loading), content pane rendering sanitized HTML, and a settings page for root override, wired to the Go API. Bundled mermaid.js and katex.js with a hydration pass that renders diagram/math placeholders in-browser with zero network calls.
- Added 8 Playwright test-loop scenarios under `e2e/` (GFM rendering, Mermaid/KaTeX hydration, path-traversal rejection, symlink-inside-root policy, CLI root override, runtime UI root override + persistence, cross-platform smoke test), plus `.github/workflows/ci.yml` (Go tests + e2e on Linux) and `release.yml` (full test gate, cross-compile matrix for windows/darwin-amd64/darwin-arm64/linux, native-runner smoke tests, GitHub Releases publish).
- Wrote the README quickstart (download, run, first-run Gatekeeper/SmartScreen workaround) and a `Makefile` `build` target, with a checked-in placeholder for `internal/assets/dist` so a fresh clone's `go build`/`go vet`/`go test` succeed before the frontend is ever built.
- `/api/tree` (and the Sidebar it feeds) now filters server-side to markdown-only files (`.md`, `.markdown`, `.mdx`, case-insensitive) and drops directories whose subtree contains no markdown anywhere below them (`.context/adr/20260711-192446-sidebar-markdown-only-filter.md`). Filtering lives entirely in `internal/rootfs/list.go` `List()`, reusing a shared `readDir` helper and a recursive `subtreeHasMarkdown` check with a symlink-cycle guard (visited real-paths set scoped per top-level `List()` call) — no changes needed to `internal/server/tree.go` or `Sidebar.tsx`. Covered by updated `internal/rootfs/list_test.go`, a new `internal/rootfs/symlink_test.go` cycle test, updated `internal/server/tree_test.go`, and a new `e2e/sidebar-tree-markdown-only-filter` scenario.
- Korean content now renders correctly across body text, headings, code blocks, Mermaid diagram labels, and sidebar filenames (`.context/adr/20260711-192651-korean-content-rendering.md`). Extended the Tailwind `--font-sans` stack in `web/src/index.css` to prepend Korean-capable system fonts (`"Apple SD Gothic Neo"`, `"Malgun Gothic"`, `"Noto Sans KR"`); wired the same resolved stack into `mermaid.initialize`'s `fontFamily` option in `web/src/hydrate.ts` since Mermaid's SVG text doesn't inherit page CSS; added a `fonts-nanum` install step to `.github/workflows/ci.yml` so the e2e suite has a Korean font on `ubuntu-latest`. Added `e2e/render-korean-content/`, covering Korean prose/heading/code-block/Mermaid-label rendering and nested Korean-named file/folder navigation via the sidebar — checks no U+FFFD replacement characters, no collapsed/empty text surfaces, correct nested-file navigation, and zero non-localhost network requests. Discovered along the way that browsers perform unconditional per-script system font fallback that can't be suppressed from web content, so pixel- or metric-based glyph-fidelity comparison (real font vs. a font known to lack Hangul) can't actually detect tofu rendering on any machine with a Korean font installed — the scenario checks structural rendering only, not glyph fidelity (confirmed once by hand via screenshot).
